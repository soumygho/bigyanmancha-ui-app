/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExaminationCentreDetailsRequestDto } from '../../models/examination-centre-details-request-dto';
import { SchoolAssignDeAssignRequest } from '../../models/school-assign-de-assign-request';

export interface DeAssignSchool$Params {
      body: SchoolAssignDeAssignRequest
}

export function deAssignSchool(http: HttpClient, rootUrl: string, params: DeAssignSchool$Params, context?: HttpContext): Observable<StrictHttpResponse<ExaminationCentreDetailsRequestDto>> {
  const rb = new RequestBuilder(rootUrl, deAssignSchool.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExaminationCentreDetailsRequestDto>;
    })
  );
}

deAssignSchool.PATH = '/api/examination-centres/de-assign';
